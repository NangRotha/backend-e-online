# Render Procfile — ប្រើសម្រាប់ "New Web Service" (Manual) ឬបើ Render រក Procfile ឃើញ
# Render នឹងដំណើរការ uvicorn នៅលើ port ដែល Render កំណត់ ($PORT)
web: uvicorn app.main:app --host 0.0.0.0 --port $PORT
