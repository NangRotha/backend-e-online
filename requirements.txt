# ============================================================
# Web framework (uvicorn[standard] = រួមទាំង websockets សម្រាប់ Real-time)
# ⚠️ ត្រូវការ [standard] ដើម្បីឱ្យ WebSocket (orders_changed, products_changed...)
#    ដំណើរការលើ Render — បើគ្មាន websockets នោះ /ws/products នឹង Error
# ============================================================
fastapi==0.111.0
uvicorn[standard]==0.30.1

# ============================================================
# Database — SQLite (built-in របស់ Python — គ្មាន Driver បន្ថែមទេ)
# ============================================================
sqlalchemy>=2.0.31,<2.1

# ============================================================
# QR Code — បង្កើតរូប KHQR ខ្លួនឯង (បើ Gateway មិនផ្តល់រូប QR មក)
# ============================================================
qrcode[pil]>=7.4,<9

# ============================================================
# Validation / Auth / Upload / Integrations
# ============================================================
pydantic>=2.9.0,<3
pydantic-settings>=2.3.0,<3
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9
httpx>=0.28.1,<1
email-validator>=2.3.0,<3
cloudinary>=1.40.0
sqids>=0.4,<1
